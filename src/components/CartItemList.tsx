import React, { PropsWithChildren } from 'react'

export type CartItemListProps = PropsWithChildren<unknown>

export default function CartItemList({}: CartItemListProps) {
   return (
      <></>
   )
}