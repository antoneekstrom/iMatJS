import React, { PropsWithChildren } from 'react'

export type ProductCardProps = PropsWithChildren<unknown>

export default function ProductCard({}: ProductCardProps) {
   return (
      <div></div>
   )
}