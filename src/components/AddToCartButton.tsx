import React, { PropsWithChildren } from 'react'

export type AddToCartButtonProps = PropsWithChildren<unknown>

export default function AddToCartButton({}: AddToCartButtonProps) {
   return (
      <></>
   )
}